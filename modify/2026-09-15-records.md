# 2026-09-15 — 홈 기록 탭(#44) 구현 ↔ 기획 문서 차이

> 이 파일은 기록일 뿐, 문서를 고치지 않았다. 폴더 규칙은 `modify/2026-09-14.md` 머리말을 따른다.

이번 작업: 이슈 #44 홈 — 기록 탭(`/records`). 디자인 `탄천런.dc.html` L829–920.

탭 3개 + 상단 기어 진입은 [2026-09-14.md](./2026-09-14.md) 1번에 이미 있어 여기서 반복하지 않는다.

---

## 1. 기록 탭 상단에 닉네임이 없다

- **문서**: "기록 탭 상단에 본인 닉네임과 함께 ① 전체 러닝 총 누적 거리 …"(`docs/04-features.md:22` F7 결과).
  `docs/07-screens.md:27` 기록 탭 「보이는 것」도 "사용자 · 닉네임(06 닉네임 칸의 쓰는 기능 F7)"을 적는다.
- **디자인 · 구현**: 상단은 설정 기어 · "내 기록" · "개인 최고 기록 및 러닝 히스토리"뿐이다(L831–839).
  닉네임을 표시하지 않았다(이슈 #44 참고 「기록 탭 상단 닉네임은 디자인에 없어 표시하지 않는다」).
- **고쳐야 할 곳**: `docs/04-features.md:22` F7 「결과」의 "본인 닉네임과 함께", `docs/07-screens.md:27` 의 닉네임 항목,
  그리고 F10 결과의 "개인 기록 화면(F7·F9)" 반영 대상에서 F7 을 뺄지(`docs/04-features.md:25`).

## 2. 페이스를 낼 수 없을 때 표기가 "—" 가 아니라 `--'--"`

- **문서**: "총 러닝 거리가 0이면 페이스를 낼 수 없으므로 숫자 대신 "—"를 보여 준다"(`docs/06-data.md:27`).
- **디자인 · 구현**: `--'--"` 다. 원본 `fmtPace` 가 0 이하 값을 `--'--"` 로 바꾼다(L1124–1125).
  이 화면에서는 기록이 없을 때 개인 최고 기록의 최고 페이스가 `--'--"` 로 보인다(이슈 #44 빈 상태 표기).
  목록 페이스도 같은 형식 함수를 쓴다(`src/app/records/format.ts` `formatPace`).
- **고쳐야 할 곳**: `docs/06-data.md:27` 평균 페이스 칸의 "—". 결과 화면 쪽 같은 문장
  (`docs/04-features.md:24` F9 ⑤ · `docs/07-screens.md:25`)은 #41 에서 판단한다.
