# 2026-09-15 — 홈 달리기 탭(+3초 카운트다운) · 구현 ↔ 기획 문서 차이

> 이 파일은 기록일 뿐, 문서를 고치지 않았다. 폴더 규칙은 `modify/2026-09-14.md` 머리말을 따른다.

이번 작업: 이슈 #42 홈 — 달리기 탭. 네 항목 모두 이슈 #42 결정 이력(2026-09-14)에서
팀이 "정본 디자인을 따르고 차이는 `modify/` 에 적는다"로 정한 것이다.

---

## 1. 카운트다운 취소를 만들지 않았다

- **문서**: `docs/03-requirements.md:29`(R16) "카운트다운 도중에 취소할 수 있어야 한다",
  `docs/04-features.md:16`(F1) "취소하면 이 화면에 그대로 남아 세션은 「시작 전」을 유지한다",
  `docs/07-screens.md:49`, `docs/05-policy.md:31` 러닝 세션 상태 전이도 같은 취소를 전제한다.
- **디자인 · 구현**: 정본 카운트다운(`탄천런.dc.html` L199-206)에는 취소 버튼이 없고,
  상태머신(L1074-1086)에도 취소 핸들러가 없다. 3 → 2 → 1 → GO! 뒤 러닝 진행으로만 간다.
  디자인에 없는 버튼을 만들지 않았다(`src/app/home/Countdown.tsx`).
- **고쳐야 할 곳**:
  - `docs/03-requirements.md:29` — R16 의 취소 요구
  - `docs/04-features.md:16` — F1 정상 흐름의 취소 문장
  - `docs/07-screens.md:49` — "3초 카운트다운 도중에 취소한다" 줄
  - `docs/05-policy.md:31` — 러닝 세션 상태 전이의 "카운트다운 도중에는 취소할 수 있고" 문장

## 2. 비준비 상태가 "GPS 확인 중" 하나뿐이다

- **문서**: `docs/05-policy.md:14`(P1)은 GPS 미확보와 위치 권한 미허용을 나눠
  각각 "GPS 신호를 확인할 수 없어 러닝을 시작할 수 없습니다" · "위치 권한을 허용해야
  러닝을 시작할 수 있습니다" 안내와 **권한 설정으로 가는 길**을 요구한다.
  `docs/04-features.md:16`(F1) · `docs/07-screens.md:47` 도 같다.
- **디자인 · 구현**: 정본의 비준비 상태는 배지 "GPS 확인 중"(L696)과 지도 딤
  "GPS 확인 중 / 러닝을 준비 중입니다"(L724-730) 하나뿐이다. 권한 미허용 분기도,
  권한 설정으로 가는 버튼도 정본에 없다. P1 의 **"시작 불가"** 자체는 지켰다 —
  시작 버튼을 `disabled` 로 두고 비활성 색으로 그린다(L1302-1305 · `RunMapCard.tsx`).
  권한 안내와 설정 경로는 위치정보 화면(#47)이 이미 담당한다.
- **고쳐야 할 곳**:
  - `docs/05-policy.md:14` — P1 의 안내 문구 두 갈래
  - `docs/04-features.md:16` — F1 예외 흐름의 권한 미허용 안내
  - `docs/07-screens.md:47` — 러닝 시작 화면 예외 줄
  - 이 항목은 `modify/2026-09-14-location.md` 5번과 같은 결이다.

## 3. 이미 진행 중인 세션(P7) 차단 안내가 없다

- **문서**: `docs/05-policy.md:20`(P7) · `docs/07-screens.md:50` 은 이미 "진행 중"인 세션이
  있으면 "이미 진행 중인 러닝이 있습니다" 안내와 함께 시작을 막으라고 한다.
- **디자인 · 구현**: 정본에 그 안내가 없고, 서버도 세션 조회도 없어 판정할 근거가 없다
  (`docs/ARCHITECTURE.md` 「아직 없는 것」). 가짜 세션 상태를 만들어 흉내내지 않았다.
- **고쳐야 할 곳**: 없다 — 문서가 틀린 게 아니라 **아직 구현하지 않은 것**이다.
  서버와 세션 조회가 붙을 때 이 화면에 더한다. 문서 수정 대상이 아니라 후속 작업이다.

## 4. 누적 거리 단위가 화면 문구로는 "탄천 누적"이다

- **문서**: `docs/04-features.md:16`(F1) 은 홈에서 보여 줄 값으로 순위와 거리를 말하지만
  라벨 문구를 정하지 않는다.
- **디자인 · 구현**: "내 탄천 순위" · "5위 / 14명" · "탄천 누적 61.0 km"(L760-768)를 그대로 썼다.
  `docs/07-screens.md` 의 화면 문구 확정과 같은 처리다(`modify/2026-09-14.md` 4번).
- **고쳐야 할 곳**: 없음. 문구를 디자인 것으로 확정한 결정에 이미 포함된다.

## 5. route `/home` · 설정 진입(기어)

각각 `modify/2026-09-15-bottomnav.md` 1번, `modify/2026-09-14.md` 1번에 이미 기록돼 있다.
중복해서 적지 않는다.
