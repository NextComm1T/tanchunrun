# 2026-09-16 — 랭킹 탭(`/ranking`) · 홈 요약 실데이터 · 구현 ↔ 기획 문서 차이

> 이 파일은 기록일 뿐, 문서를 고치지 않았다. 폴더 규칙은 `modify/2026-09-14.md` 머리말을 따른다.

이번 작업: 이슈 #87 [Backend B7] 누적 랭킹 · 홈 요약 실데이터.

D6(누적 source) · D7(정렬 · 스냅샷) · 「최초 달성 시각」이 문서와 갈라지는 지점은 #85 가
[modify/2026-09-16-run-finish.md](./2026-09-16-run-finish.md) 2 · 3 · 4번에 이미 적어 뒀다.
이 화면들은 그 규칙을 **소비만** 하므로 여기서 다시 적지 않는다.

---

## 1. 빈 목록 · 본인 미등재가 이제 실제로 성립한다 (상태 승격 기록)

- **문서**: `docs/04-features.md` F5 · `docs/05-policy.md` P5 — "누적 탄천 인정 거리가 0보다 큰
  사용자만 랭킹에 오른다". `docs/07-screens.md:26` 이 빈 상태 문구를 정한다.
- **디자인 · 구현**: 정본 `탄천런.dc.html` L772-828 에는 두 상태가 **없다**(항상 14명 · 본인 5위).
  [modify/2026-09-15-ranking.md](./2026-09-15-ranking.md) 1번이 적어 둔 그대로인데, 그때는
  `?state=` 쿼리로 골라 보던 것이 이제 **실제 조회 결과**다 — 아무도 인정 거리를 쌓지 않았으면
  빈 목록이고, 내 누적이 0 이면 내 순위 카드가 렌더되지 않는다(`src/app/ranking/page.tsx`).
- **고쳐야 할 곳**: 없음. 문서가 맞고 디자인이 그 경우를 안 그렸을 뿐이다.

## 2. 홈 「내 탄천 순위」에 순위가 없는 상태가 디자인에 없다

- **문서**: `docs/05-policy.md` P5 — 누적 0 인 사용자는 랭킹에 오르지 않는다. 그러면 홈 카드에
  넣을 순위 숫자도 없다. `docs/07-screens.md` 의 홈 화면에는 이 경우의 표기가 없다.
- **디자인 · 구현**: 정본 L760-768 은 늘 「5위 / 14명」이 차 있다. 새 문구나 레이아웃을 만들지
  않고 **숫자 자리만 「-」로** 두고 「/ N명」과 「탄천 누적 0.0km」는 그대로 뒀다
  (`src/app/home/MyRankCard.tsx`). 랭킹 탭이 본인 줄을 그냥 빼는 것과 같은 결의 최소 표현이다.
- **고쳐야 할 곳**: `docs/07-screens.md` 홈 화면 「보여 주는 것」 — 순위 없음 표기를 정할지
  디자인 담당이 정한다. 문구를 새로 만들지 않았으므로 문서를 고치지 않아도 어긋나지는 않는다.

## 3. 홈 · 랭킹이 같은 조회를 지난다 (차이 아님 · 확인 기록)

`docs/04-features.md` F5 와 `docs/07-screens.md:26` 은 순위를 "저장 항목 아님 — 누적 탄천 인정
거리와 최초 달성 시각으로 산정하는 값"으로 정한다. 구현도 그대로다 — 순위 · 정렬 · 0 제외는
`src/server/ranking`(#85)이 한 곳에서 하고, `src/server/rankingView.ts` 는 거기에 닉네임과
`isMe` 만 붙인다. 홈 카드도 **같은 함수**를 지나므로 `/ranking` 본인 줄과 다른 값이 나올 수 없다.
`modify/2026-09-15-ranking.md` 7번이 예고한 "서버가 붙어도 이 자리가 그대로 조회 결과 자리가
된다"가 그대로 됐다. 고칠 문서 없음.

## 4. 홈에 닉네임 없는 사용자가 들어오면 `/` 로 돌려보낸다 (차이 아님 · 확인 기록)

`docs/07-screens.md:35-38` 의 진입 분기는 `/` 가 갖고 있다(#80). `/home` 을 직접 열면 그 분기를
건너뛰는데, 인사에 넣을 닉네임이 없고 러닝도 시작할 수 없는 상태라(`src/server/runs/actions.ts:61`)
홈을 그릴 이유가 없다. 어디로 보낼지 여기서 정하지 않고 `/` 로 넘겨 기존 분기가 판단하게 했다
(`src/app/home/page.tsx`). 문서가 정한 분기를 그대로 쓰는 것이라 고칠 문서 없음.
