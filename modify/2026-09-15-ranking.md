# 2026-09-15 — 홈 랭킹 탭(`/ranking`) · 구현 ↔ 기획 문서 차이

> 이 파일은 기록일 뿐, 문서를 고치지 않았다. 폴더 규칙은 `modify/2026-09-14.md` 머리말을 따른다.

이번 작업: 이슈 #43 홈 — 랭킹 탭.

---

## 1. 빈 목록 · 본인 미등재 상태가 정본 디자인에 없다

- **문서**: `docs/04-features.md` F5(랭킹 조회) · `docs/05-policy.md` P5 는 "누적 탄천 인정 거리가
  0보다 큰 사용자만 랭킹에 오른다"고 정한다. `docs/07-screens.md:26` 이 빈 상태 문구
  "아직 랭킹 데이터가 없습니다"를 적는다.
- **디자인 · 구현**: 정본 `탄천런.dc.html` L772-828 에는 **두 상태 모두 없다** — 항상 14명이
  차 있고 본인도 5위로 들어가 있다(L969-983). 논리적으로 반드시 생기는 상태라 새 일러스트 없이
  기존 토큰 · 타이포로만 최소 표현했다. 빈 목록은 `text-content text-muted` 한 줄,
  본인 미등재는 내 순위 카드를 렌더하지 않는 것으로 끝낸다(`src/app/ranking/page.tsx`).
- **고쳐야 할 곳**: 없음. 문서가 맞고 디자인이 그 경우를 안 그렸을 뿐이다. 정본을 다시 캡처할 때
  두 상태를 추가할지 디자인 담당이 정한다.

## 2. 1·2·3위가 `rank-*` 색 토큰이 아니라 메달 이미지다

- **문서**: `docs/ARCHITECTURE.md` 색 표가 "랭킹 1·2·3위 → `text-rank-gold` · `-silver` ·
  `-bronze`" 로 적는다. 토큰도 `src/app/globals.css:62-64` 에 있다.
- **디자인 · 구현**: 정본 L803-811 은 순위 숫자 대신 `assets/medal-{1,2,3}.png` 를 42×50 으로
  그린다. 그래서 이 화면은 `rank-*` 토큰을 **한 번도 쓰지 않는다**. 토큰은 지우지 않았다 —
  기록 상세(#45) 등 다른 화면이 쓸 수 있다(이슈 #43 결정 이력 2026-09-14).
- **고쳐야 할 곳**: `docs/ARCHITECTURE.md` 색 표의 "랭킹 1·2·3위" 줄 — 쓰는 화면을 특정하거나
  "정본 랭킹 탭은 메달 PNG 를 쓴다"를 덧붙인다.

## 3. 상단이 공용 `Header` 가 아니다

- **문서**: `docs/SCREEN_ASSIGNMENTS.md` 공통 완료 조건(이슈 #68 동기화 **이전** snapshot)이
  "`AppShell` · `Header` 를 쓰고" 로 적혀 있었다.
- **디자인 · 구현**: 정본 L774-782 의 상단은 sticky 도, 아래 테두리도 없고 제목이 32px 다 —
  공용 `Header`(sticky · 테두리 · 20px 제목)와 구조가 다르다. 화면 폴더 안에 로컬로 그렸고
  `src/components/shared/*` 는 건드리지 않았다.
- **고쳐야 할 곳**: 없음. #68 이 이미 `develop` 에서 "공용 `Header` 는 구조가 맞을 때만" 으로
  고쳤고(`docs/SCREEN_ASSIGNMENTS.md` · `docs/ARCHITECTURE.md` "새 화면 만들기 3단계"),
  이슈 #43 AC 와도 일치한다. 이 줄은 `main` 이 develop 을 받으면 같이 해소된다.

## 4. 설정 기어를 `<button onClick>` 이 아니라 링크로 구현

- **문서 · 디자인**: 정본 L779 는 `<button onClick="{{ onSettings }}">` 이다.
- **구현**: `next/link`. 디자인의 `onClick` 은 단일 HTML 안에서 도는 데모 상태머신용이고 실제
  앱에서는 route 이동이라 링크가 맞다 — `modify/2026-09-15-bottomnav.md` 2번과 같은 사유다.
  덕분에 이 화면은 `"use client"` 없이 전부 서버 컴포넌트로 끝난다. 시각 결과는 동일하다.
- **고쳐야 할 곳**: 없음.

## 5. 내 순위 카드 그림자에 딱 맞는 토큰이 없다

- **디자인**: L784 `box-shadow: 0 4px 14px rgba(47,111,232,0.24)`.
- **구현**: `shadow-primary`(`globals.css:115` = `0 4px 12px rgb(47 111 232 / 0.25)`). blur 2px ·
  alpha 0.01 차이라 육안으로 구분되지 않고, 새 토큰을 만들면 전원이 공유하는 `globals.css` 를
  건드려야 해서 기존 토큰을 그대로 썼다(`docs/SCREEN_ASSIGNMENTS.md` 충돌 방지).
  목록 행 그림자(L801 `0 2px 8px rgba(30,46,79,0.05)`)는 `shadow-button-soft` 와 **값이 정확히 같다**.
- **고쳐야 할 곳**: 없음. 토큰을 늘릴 만한 차이가 아니라고 판단했다 — 다르게 보면 팀이 정한다.

## 6. 문서는 "본인 순위를 따로 요약하지 않는다"고 못 박는데 정본에는 요약 카드가 있다

- **문서**: `docs/04-features.md:20` F5 결과 — "**목록에서는 본인 줄을 강조해 보여 준다**(본인 순위를
  따로 요약해 두지는 않는다 — 05 에 없는 화면 규칙이라 04 와 07 이 단일 기준이다)".
  `docs/07-screens.md:26` 도 「목록에서 본인 줄 강조」만 적고 요약 카드를 적지 않는다.
- **디자인 · 구현**: 정본 L784-797 에 **sticky 내 순위 강조 카드**(solid primary 배경 · 흰 글자 ·
  순위 · 닉네임 · 누적 거리)가 있다. 이슈 #43 AC 와 결정 이력(2026-09-14)도 이 카드를 요구한다.
  목록 안 본인 줄 강조(L1230-1232)는 **그것대로 따로** 구현했으므로 문서가 말한 것을 뺀 게 아니라,
  문서가 없다고 한 것이 정본에 더 있는 경우다.
- **고쳐야 할 곳**:
  - `docs/04-features.md:20` — F5 결과의 "(본인 순위를 따로 요약해 두지는 않는다)" 괄호
  - `docs/07-screens.md:26` — 랭킹 화면 「보여 주는 것」에 본인 순위 요약 카드 추가

## 7. 순위와 tiebreak 는 문서대로 저장하지 않고 계산한다 (차이 아님 · 확인 기록)

`docs/07-screens.md:26` 이 순위를 "**저장 항목 아님** — 누적 탄천 인정 거리와 랭킹 집계 ·
최초 달성 시각으로 산정하는 값 · P5 · P8" 으로 정한다. 구현도 그대로다 — `mock.ts` 는 순위 숫자를
갖지 않고 `distance` · `firstReachedAt`(= `docs/06-data.md:44` 「랭킹 집계 · 최초 달성 시각」)만
들고 있으며, `page.tsx` 의 `toRanking()` 이 P5 필터 → P5·P8 정렬 → index 로 순위를 만든다.
고칠 문서 없음. 서버가 붙어도 이 자리가 그대로 조회 결과 자리가 된다.
