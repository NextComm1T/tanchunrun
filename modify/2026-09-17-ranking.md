# 2026-09-17 — 홈 랭킹 탭(`/ranking`) · 빈 랭킹 · 본인 미등재 안내 (#121)

> 이 파일은 기록일 뿐, 문서를 고치지 않았다. 폴더 규칙은 `modify/2026-09-14.md` 머리말을 따른다.
> `modify/2026-09-15-ranking.md` 가 같은 화면의 이전 차이를 기록해 뒀다 — 이 파일은 #121 이
> 새로 더한 것만 적는다.

## Ranking Zone 참여 조건 설명 · `/home` CTA · 본인 미등재 안내가 정본에 없다

- **문서**: `docs/04-features.md` F5 는 빈 상태에서 "아직 랭킹 데이터가 없습니다" 문구만
  정하고, 참여 조건 설명이나 CTA · 본인 미등재 전용 안내는 적지 않는다. `docs/07-screens.md:26`
  도 같다.
- **디자인 · 구현**: 정본 `탄천런.dc.html` L772-828 은 두 상태(빈 목록 · 본인 미등재) 자체가
  없다 — `modify/2026-09-15-ranking.md` 1번이 이미 기록한 대로 항상 14명이 차 있고 본인도
  5위로 들어가 있다. #121 은 그 위에 다음을 더했다(`src/app/ranking/page.tsx`):
  - 빈 랭킹 상태에 참여 조건 설명("탄천 Ranking Zone 안에서 달리면 인정 거리가 쌓여
    랭킹에 올라요")과 `/home` CTA("달리러 가기")를 추가했다.
  - 본인 누적 인정 거리가 0이라 목록에 없을 때(다른 사용자는 있음), 이전에는 아무 표시 없이
    내 순위 카드만 사라졌다. 이제 같은 모양의 안내 카드("아직 탄천 Ranking Zone 인정 거리가
    없어요" + 같은 CTA)를 보여 준다.
  - 새 일러스트 없이 기존 카드 토큰(`rounded-2xl border-border bg-surface shadow-card` —
    `InfoCard` 류와 같은 값)과 기존 CTA 버튼 토큰(`RunMapCard.tsx` 의 위치정보 설정 링크와
    같은 `bg-primary text-on-primary rounded-xl`)을 재사용했다(#43 방침을 그대로 따름).
  - **가짜 순위는 만들지 않는다** — 두 안내 모두 순위 숫자를 표시하지 않는다.
  - ranking server query(`src/server/rankingView.ts`)는 건드리지 않았다.
- **고쳐야 할 곳**:
  - `docs/04-features.md:20` F5 — 빈 상태 설명에 참여 조건 · CTA 를 덧붙인다.
  - `docs/07-screens.md:26` — 본인 미등재 상태의 별도 안내를 상태 목록에 추가한다.
  - 정본을 다시 캡처할 때 디자인 담당이 이 두 상태를 반영할지 정한다
    (`modify/2026-09-15-ranking.md` 1번과 같은 결론).
